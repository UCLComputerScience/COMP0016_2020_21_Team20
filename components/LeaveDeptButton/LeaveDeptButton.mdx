---
name: LeaveDeptButton
route: /LeaveDeptButton
menu: Non-page specific
---

import { Playground, Props } from 'docz';
import LeaveDeptButton from './LeaveDeptButton';
import 'rsuite/dist/styles/rsuite-default.min.css';

# LeaveDeptButton

The `LeaveDeptButton` component is used in the header to allow users to leave their department in case they are quitting or are joining a different one.

<Props of={LeaveDeptButton} />

It is normally used in conjunction with the [`Header`](/Header) wrapper component.

## Basic usage

<Playground>
  <LeaveDeptButton />
</Playground>
