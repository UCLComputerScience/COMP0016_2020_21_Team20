---
name: LoginMessage
route: /LoginMessage
menu: Homepage
---

import { Playground, Props } from 'docz';
import LoginMessage from './LoginMessage';
import 'rsuite/dist/styles/rsuite-default.min.css';

# LoginMessage

The `LoginMessage` component is used on the homepage when the user is not logged in to guide them on how to begin by registering/logging in.

<Props of={LoginMessage} />

It is normally used in the Homepage.

## Basic usage

<Playground>
  <LoginMessage />
</Playground>
